import type { FC } from 'react';

interface InputDropProps {
    Array:any[]
}

const InputDrop: FC<InputDropProps> = ({Array}) => {
    return (
        <div>
            
        </div>
    );
}

export default InputDrop;
